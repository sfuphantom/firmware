/*
 * RTD.c
 *
 * Created on: Jan 20, 2021
 *     Author: Adam Tinaburri
 *
 * To do check-list
 *      - Initialize gio, vim, gio notification
 *      -
 */

#include "RTD.h"

/*
void main(void)
{
    RTDInit();

	while(1){
	    if(isBreakPressed() && isTractionActive()){
	           gioToggleBit(gioPORTA, 5);
	           // RESET INTERRUPT REGISTER(S)
	       }
	}
}


void RTDInit(){
    gioInit();

    vimInit();

    gioEnableNotification(gioPORTA, 1);
}

bool isBreakPressed() // checks if the break is being pressed
{
    return 1; // jay's code, look at his function, can call it so see if the brake sensor is active
}

bool isTractionActive() // checks if tractive system is active
{
    return 1; // GIO getBit, see if the tractive system is active is a 1 or 0
}
*/







